export interface SchoolRecord {
  id: string;
  month: string;
  camp: string;
  country: string;
  school: string;
  lessons: number;
}

export interface LegendSummary {
  total: number;
  selectedCamp: string;
}
