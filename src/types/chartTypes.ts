export interface ChartSelectedElement {
  selectedDataset: string;
  selectedLabel: string;
}

export interface PointDefaultOptions {
  pointStyle: string;
  pointBackgroundColor: string;
  pointRadius: number;
  pointHoverRadius: number;
  spanGaps: boolean;
}
